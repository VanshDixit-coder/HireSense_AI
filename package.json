{
  "name": "hiresense-ai",
  "version": "1.0.0",
  "private": true,
  "description": "HireSense AI full-stack application",
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"PORT=5001 npm run dev --prefix server\" \"VITE_API_URL=http://localhost:5001/api npm run dev --prefix client -- --host 127.0.0.1\"",
    "install:all": "npm install && npm install --prefix server && npm install --prefix client",
    "seed": "npm run seed --prefix server",
    "build": "npm run build --prefix client",
    "start": "npm start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
